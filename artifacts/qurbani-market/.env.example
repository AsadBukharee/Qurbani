# Mobile app environment variables.
# Copy to .env (gitignored) and fill in.

# Django REST backend URL (no trailing slash). Leave blank to keep
# AsyncStorage-only mode (auth/listings/etc. stay on-device).
# Example: https://api.qurbani-market.com/api
EXPO_PUBLIC_API_URL=
